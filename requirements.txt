# Requires Python 3.12
openai-whisper
transformers
torch
soundfile
sentencepiece
# NumPy 2.x currently breaks binary wheels used by PyTorch and Whisper.
# Install explicitly with: python -m pip install "numpy<2"
numpy<2
pytest
